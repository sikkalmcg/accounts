# TODO - IRN01 / IRN02 Mandatory & Duplicate Validation

## Steps
- [x] 1. Update duplicate-validator.ts error messages for irnNumber/ackNo to match spec
- [x] 2. IRN01: add mandatory-field validation (IRN, ACK No, ACK Date, QR Code) + cross-plant duplicate checks + normalization
- [x] 3. IRN02: add mandatory-field validation in handleSave + duplicate checks (with exclusion) + normalization
- [x] 4. Run TypeScript check (npx tsc --noEmit)

